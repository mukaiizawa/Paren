name: punit
on: [push]
jobs:
  unit-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: make os=unix debug=on
        working-directory: ./src
      - run: ./paren manunit
      - run: ./paren punit -r ./wk
