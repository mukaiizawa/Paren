# NAME
address - object address.

# SYNOPSIS

    (address X)

# DESCRIPTION
The function `address` returns a address of `X`.

# RETURN VALUE
Returns a address.

# NOTES

    (== X Y) <=> (= (address X) (address Y))

# SEE ALSO
- ==(3)
